{
  "name": "pipeline-orchestrator",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "飞书 + Claude Code 开发流水线编排服务：状态机路由 + headless 会话调度 + 交互端口",
  "scripts": {
    "orchestrate": "tsx src/cli.ts",
    "daemon": "tsx src/daemon.ts",
    "webhook": "tsx src/gitlab/service.ts",
    "pause": "node scripts/pause.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.73.0",
    "ajv": "^8.17.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
