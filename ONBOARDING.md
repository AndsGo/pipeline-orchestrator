# 飞书开发流水线 · 新人上手

> 你只需要会：在对的群里，用中文把事说清楚。剩下的（澄清、计划、实现、评审、CI、验收、知识沉淀）流水线自己跑，需要你拍板时会弹卡片。

## 一、群的玩法（2026-09 起的多群结构）

| 群 | 用途 |
|---|---|
| **主群** | 综合入口与管理：接新项目、跨项目查询、看板 |
| **项目群**（一群一项目） | 该项目的一切：提需求、问问题、跑分析；该项目工单的通知/卡片也只发这里 |

**给项目开专属群**：建个飞书群 → 把机器人拉进去 → 在群里发 `/bind 项目别名`。完成。从此这个群里说的话默认归该项目，不用每句报项目名。

**在主群指定项目**：消息里带上项目名（拼错了会弹卡确认），或 `/use 项目别名` 设定几小时内的默认项目（`/use` 查看当前）。每次执行的回执都会写明「在 X 上执行」，看到不对随时纠正。

## 二、两条通道：什么事走哪条

**研究性/一次性的活 → 直接说（或 `/run`）**：查数、导表、看代码、验收核对、分析根因。几毛钱几分钟出结果，不建工单。会话可读生产只读库、可跑测试，**但没有改代码的权限**——这是刻意的。

> 例：`/run 查生产只读库，导出 8 月之后缺 SPU 的 listing 清单，CSV 写到 D:/work/exports/ 下`

结果结尾如果列了编号问题，直接回复（如「1 要；2 不用」）就能接着办。

**要改代码/留下功能 → `/new 一句话需求`**：走全流程（澄清→计划→实现→评审→CI→验收→知识沉淀），全程有留痕有回滚。你刚用 /run 排查完的结论会**自动附进新工单**，不用手动搬运。

**边界口诀**：只查不改走 /run；要改代码走 /new；拿不准先 /run 诊断，需要改再升级建单。需求里写明「一次性/只读/不改代码」，分诊会走轻量通道。

## 三、工单跑起来之后你会遇到什么

- **卡片**：澄清问题（逐条答或「全部通过」）、计划审批、上线审批、验收确认（逐条打勾）、知识采纳。点按钮或直接打字都行。
- **结果预览**：PRD 确认卡上会带一个「📱 结果预览」链接——点开是一页看得见的东西（界面原型 / 假数据的结果表样例 / 流程图和实例），不用啃文字 PRD。注意页顶的提示：**它是示意不是承诺**，做没做到以页底的验收标准清单为准。看完对着卡片点通过/驳回；有任何意见直接在群里说。
- **查看**：`/status LS-004` 单个工单时间线；`/list` 全部工单；`/dashboard` 总览面板（链接/成本/指标）。
- **改需求**：`/amend LS-004 需求改成…`（会回退到澄清重跑，有确认卡）；只是补充说明用 `/note LS-004 …`（不回退）。
- **中断恢复**：机器人重启后会自动点名被打断的工单和失效的卡片，照提示发「继续 LS-004」即可，进度不丢。

## 四、接新项目（管理员）

群里一条命令：

```
/addproject 别名 D:/work/仓库路径 前缀 gitlab=组/项目 jenkins=CI任务 wiki=归档节点
```

后三项可省（gitlab/wiki 直接粘 URL 也行）；即时生效。之后建议：终端跑 `npx tsx scripts/doctor.ts` 体检、`npx tsx scripts/system-map.ts --rebuild 别名` 生成业务能力地图（这是澄清阶段洞察力的来源）、目标仓库放一份 CLAUDE.md 和 PROJECT-BRIEF.md。最后按第一节给它开个专属群。

## 五、几条实话

- 每个项目的 CI 任务要自己配（`jenkins=` 字段），没配的项目工单评审通过后直达验收——**绝不会**借用别的项目的构建。
- /run 会话是无人值守的：它做不到的事会直说并给替代路径，不会假装完成。
- 命令拼错不用慌：`/dashborad` 这类手滑会收到「你是不是想说 /dashboard？」。
- 知识库每月自动做老化审计并发群；确认过时的条目在知识表把状态改「已失效」即可。
