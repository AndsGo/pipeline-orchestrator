# 安全说明

## 这套系统握着什么

编排器运行时持有一组高权限凭据，并会在你的代码仓库里读写、提交、合并。部署前请清楚它能做什么。

| 凭据 | 用途 | 最小权限 |
|---|---|---|
| `FEISHU_APP_SECRET` | 飞书长连接、发卡、读群消息、写多维表格与云文档 | 自建应用；按实际启用的功能开权限（见 `docs/getting-started.md`） |
| `GITLAB_API_TOKEN` | 找 MR、合并 MR、MR 评论回帖 | **个人访问令牌**（PAT）`api` 范围。项目访问令牌只能看到它所属的一个项目，多项目部署会静默找不到 MR |
| `JENKINS_TOKEN` | 触发构建、轮询结果、取日志 | 用户 API token（免 CSRF crumb） |
| 模型 API | 阶段会话（`claude` / `codex` CLI 自带的登录态） | 由 CLI 管理，编排器不接触 |

**编排器会以这些凭据自动执行的不可逆动作**：合并 MR 到目标分支（上线审批通过后）、提交并推送 `docs/pipeline/` 工件、经 MR 合入 `CLAUDE.md` 建议。每一个都前置了人工卡点或人审卡。

## 凭据卫生

- `.env` 是明文，已在 `.gitignore`。**永不入库、永不贴进聊天**。日志与文档里只出现键名、有无、长度。
- `data/` 含工单状态、事件流、`/run` 会话的完整输出（可能包含从生产只读库查出的数据）、下载的引用图片/文件。已 `.gitignore`；备份 zip 在 `backups/`，同样不入库。请把执行机的磁盘当敏感存储对待。
- 轮换凭据时只改对应行，保持 UTF-8——曾因重写文件丢掉大半配置。
- 调试期间经聊天渠道传过的任何 token，投产前全部重置。

## 阶段会话的权限边界

- 每个阶段的工具白名单钉死在 `src/config.ts`（如评审阶段没有 `Edit`）。`/run` 单次执行没有写工具，且提示词明确禁止用 Bash 绕过。
- Codex 引擎没有按工具的白名单，只有沙箱级别：白名单含写工具 → `workspace-write`，否则 `read-only`。
- 阶段会话跑在目标仓库的 worktree 里，`--settings config/pipeline-settings.json` 禁用了会注入自身流程规则的元框架层。
- 会话对外副作用（部署、推送）由分类器识别，一律先弹确认卡；这不依赖斜杠语法，自然语言"帮我推一下"同样拦。

## 网络面

- 除 `GITLAB_WEBHOOK_PORT`（默认 8377）外全部出站。该端口承载 GitLab webhook（有 `GITLAB_WEBHOOK_SECRET` 校验）与结果预览页（`GET /preview/<ticket>/…`，有路径穿越防护，只读工单目录下的 `prototype/`）。
- 预览页无鉴权，**只应暴露在内网**。

## 报告漏洞

请不要在公开 issue 里描述可利用的细节。发邮件到仓库维护者（见 git 提交作者），说明影响范围与复现条件；会在合理时间内回复。修复后会在 CHANGELOG 里致谢（如你愿意）。
